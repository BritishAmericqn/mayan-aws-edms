from mayan.apps.dependencies.classes import PythonDependency

# Research app dependencies - only adding new ones not in production
# pandas, matplotlib, openpyxl, reportlab - these will be loaded only if needed

